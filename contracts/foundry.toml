[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
injective_testnet = "${INJECTIVE_EVM_RPC_URL}"

[etherscan]
# Injective's inEVM explorer verification endpoint, if/when available for the target network.
